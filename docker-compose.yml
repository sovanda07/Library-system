services:

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - auth_service
      - book_service_1
      - book_service_2
      - borrow_service
    networks:
      - library-network

  auth_service:
    image: ${DOCKER_USERNAME}/auth-service
    build:
      context: .
      dockerfile: Auth_service/Dockerfile
    environment:
      - MONGO_URI=${MONGO_URI}
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - library-network

  book_service_1:
    image: ${DOCKER_USERNAME}/book-service
    build:
      context: .
      dockerfile: Book_service/Dockerfile
    environment:
      - MONGO_URI=${MONGO_URI}
      - JWT_SECRET=${JWT_SECRET}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis
    networks:
      - library-network

  book_service_2:
    image: ${DOCKER_USERNAME}/book-service
    build:
      context: .
      dockerfile: Book_service/Dockerfile
    environment:
      - MONGO_URI=${MONGO_URI}
      - JWT_SECRET=${JWT_SECRET}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis
    networks:
      - library-network

  borrow_service:
    image: ${DOCKER_USERNAME}/borrow-service
    build:
      context: .
      dockerfile: Borrow_service/Dockerfile
    environment:
      - MONGO_URI=${MONGO_URI}
      - JWT_SECRET=${JWT_SECRET}
      - BOOK_SERVICE_URL=http://nginx
    depends_on:
      - book_service_1
      - book_service_2
    networks:
      - library-network

  redis:
    image: redis:latest
    networks:
      - library-network

networks:
  library-network:
    driver: bridge